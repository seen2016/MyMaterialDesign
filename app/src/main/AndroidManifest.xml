<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seen.mvp.mymaterialdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThem">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PaletteActivity" />
        <activity android:name=".TranslucentActivity" />
        <activity android:name=".Translucent2Activity" />
        <activity android:name=".BehaviorFABActivity" />
        <activity android:name=".AppBarLayoutActivity" />
        <activity android:name=".NestedScrollViewActivity" />
        <activity android:name=".NestedScrollView2Activity" />
        <activity android:name=".CustomBehaviorActivity" />
    </application>

</manifest>